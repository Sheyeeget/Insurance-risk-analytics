"# Insurance Risk Analytics - ACIS" 
## Reproducing the data pipeline

1. Install DVC: `pip install dvc`
2. Pull data from DVC remote: `dvc pull`
3. (Optional) To switch versions: `dvc checkout data/datainsurance_data.csv.txt.dvc`
